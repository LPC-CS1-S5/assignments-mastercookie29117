#include <iostream>
#include <cstdlib>
#include <ctime>

using namspace std;

int main()
{
  int num1, num2, num3, num4;

  srand(time(0));

  num1 = rand() % 100;
  num2 = rand() % 100;
  num3 = rand() % 100;
  num4 = rand() % 100;
  min = num1


  if (num1 > num 2)
   min = num2; 
  if (min > num 3)
    min = num3;
  if (min > num4)
    min = num4;
  
  cout << "The random numbers are :" << num1 <<" " << num2 << " " << num3 << " " << num4 << " and the minimum value is : " << min << " ." << endl; 

  return 0; 
  

}