#include <iostream>
using namespace std;

int main()
{
  int num1, num2, num3, min, max; 

  cout << "Enter three numbers: "<< endl;
  cin >> num1>> num2 >> num3>> endl;

  min = num1
  midd = num2
  max = num3
  

  if (num1 > num2)
    min = num2 
  else 
    min = num1 
  if (num2 > num3) 
    min = num3 
  else
    min = num2
  if (num2 > min ) || (num2 < max)
    midd = num2
  
  
  cout << "The minimum value is " << min << " the middle valve is " << midd << " and the max number is " << max << " ." << endl;




  


}