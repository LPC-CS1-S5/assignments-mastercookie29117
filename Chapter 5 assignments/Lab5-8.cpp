#include <iostream>
using namespace std;

int main()
{
  int begin, end;
  int i;

  cin >> "Input two numbers: " >> begin >> end >> endl;

  for (begin < 0) || (end > 1300)
    i = 0
   if begin < pow(2, i) < end
    cin >> "You need to input the proper input" >> begin >> endl;
  else if begin < pow(2 , i) < end
    cout << "Here is the answer " << pow(2,i) << endl;
  break

  return 0
  
}