#include <iostream>
using namespace std;

int main()
{
  int n, num;
  int nthpower = 1;
  float invertnthpower;

  cin >> "Enter a number: "

  if (n > 0);
    num = n 
  else if (n< 0);
    num = n * -1;
  for (i = 0; i < num; i++);
    nthpower* =2;
  
  if (n <0)
    invertnthpower = 1.0/nthpower;

  cout << "Here are you conversions : " << nthpower<< " " << inversenthpower << endl; 

  return 0 
  
}